#include <iostream>
int gsd(int a, int b)
{
    int r;
    do
    {
        r = a % b;
        a = b;
        b = r;
    } while (r != 0);
    if (a == 0)
    {
        return b;
    }
    else if (b == 0)
    {
        return a;
    }
}
int main()
{
    std::cout << gsd(75, 45);
}
